  <% if @posts.any? %>
  <div class="col-md-8 mx-auto">
    <ul class="posts">
      <%= render @posts %>
    </ul>
    <%= paginate @posts %>
  </div>
  <% else %>
  <p>投稿が見つかりません</p>
  <% end %>

  <style>
    .posts {
      display: flex;
      flex-wrap: wrap;
      /* justify-content: space-between; */
    }

    .posts li:not(:last-child) {
      margin-right: 20px;
    }

    .posts li {
      width: 30%;
    }
  </style>