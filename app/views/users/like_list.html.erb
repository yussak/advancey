<% provide(:title, "いいね一覧") %>
<h1><%= @user.name %>さんのいいね一覧</h1>
<div class="col-md-6 mx-auto">
  <% if @likes.any? %>
  <ul class="feed_list">
    <li><%= render @likes %></li>
  </ul>
  <%= will_paginate @likes %>
  <% else %>
  <p>まだいいねしていません</p>
  <% end %>
</div>